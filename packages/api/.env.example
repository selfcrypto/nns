# packages/api — environment
#
# Copy to .env and fill in. .env is gitignored; no secrets in the repo.
#   cp packages/api/.env.example packages/api/.env
#   pnpm --filter @nns/api dev
#
# The API holds no keys, applies no protocol rules, and reads no §3 value
# from the environment (everything §3-shaped it serves is core's CONSTANTS —
# see below). It needs a database to read, and little else.

# ── Database (required) ──────────────────────────────────────────────────────
# The indexer's Postgres — or a read replica of it; every query runs in a
# READ ONLY transaction and the API never migrates.
NNS_DATABASE_URL=postgres://nns:nns@127.0.0.1:5433/nns

# ── HTTP bind (optional; defaults shown) ─────────────────────────────────────
# Loopback by default. A container sets NNS_API_HOST=0.0.0.0 explicitly.
NNS_API_HOST=127.0.0.1
NNS_API_PORT=8635

# This service reads no §3 value from the environment. Everything §3-shaped
# it serves — RESERVED_NAMES for /available, the O listing fee for /params,
# the §10.2 burn values for /burn — is core's CONSTANTS, so nothing here can
# drift from the indexer's.

# ── Tuning (optional; defaults shown) ────────────────────────────────────────
NNS_LOG_LEVEL=info
