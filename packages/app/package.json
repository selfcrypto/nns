{
  "name": "@nns/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Nimiq Pay mini app: search, resolve, register and manage NNS names. Static Vite bundle, independently hostable.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/copy-resolver-bundle.mjs",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@nimiq/hub-api": "catalog:",
    "@nimiq/identicons": "catalog:",
    "@nimiq/mini-app-sdk": "catalog:",
    "@nns/chat": "workspace:*",
    "@nimiqnames/core": "workspace:*",
    "@nimiqnames/resolver": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "marked": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
