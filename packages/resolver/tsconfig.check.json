{
  // Typecheck everything, including tests and fixtures.
  // tsconfig.json is the narrower build config: it emits src/ only.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src/**/*.ts", "vitest.config.ts"]
}
