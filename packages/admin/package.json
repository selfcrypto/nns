{
  "name": "@nns/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cold-key CLI for P (governance), U (unreserve) and F (burn attestation). P and U are implemented; F is not yet.",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.check.json",
    "test": "vitest run",
    "start": "node --env-file=.env dist/main.js"
  },
  "dependencies": {
    "@nns/core": "workspace:*",
    "@nns/indexer": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
